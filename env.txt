# Move secrets to local secret manager / deployment env vars.
# Commit only placeholders in an example file, e.g.:
# JWKS={"keys":[...redacted...]}
# JWT_PRIVATE_KEY={...redacted...}
# AUTH_SECRET=your-auth-secret-here
